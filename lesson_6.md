1. Pull request на github.com
   - после надо стянуть изменения `git pull origin main`
2. Python Package или новая директория и `__init__.py`
3. `.gitignore` в корневой папке
4. запуск через терминал: 
    - `python lessons/lesson_6.py`
    - `python -m lessons.lesson_6`(запуск как модуля)
    - `python main.py`(main.py в корневой папке)
